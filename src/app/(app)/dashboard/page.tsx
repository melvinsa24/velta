import { ScreenHeader } from '@/components/layout/ScreenHeader'

// Écran squelette — le contenu du dashboard arrive en Phase 8.
export default function DashboardPage() {
  return <ScreenHeader title="Dashboard" subtitle="Mois en cours" />
}
